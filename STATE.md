# STATE — состояние проекта RemoteDeskPBX

Обновляется в конце каждой сессии: что сделано, что осталось, как проверить.

## Дата: 2026-07-14

### Сделано
- Сервер/админка развёрнуты на Render из `remotedeskpbx-server` (Express, `/admin`, пароль admin123).
- Разделение на два приложения Client / Employee, portable `.exe` в `release/`.
- **T3** — убрано меню Electron (чистое окно) в обоих приложениях.
- **T6** — вставка ID даёт ровно 9 цифр.
- **T9** — в админке активная вкладка следует за кликом; клик по сессиям сотрудника открывает их список. Задеплоено на прод.
- **T10** — сессия привязывается к сотруднику (`/join` шлёт login/name, сервер пишет в сессию). Сервер задеплоен; employee.exe пересобран.
- Аудио клиента через loopback; фикс «Could not start audio source».
- Заведён процесс: `BACKLOG.md`, `CLAUDE.md`, `SESSION_PROMPT.md`, этот файл.

### Осталось (см. BACKLOG.md)
- **T1** — сохранение пароля сотрудника / авто-вход (P1).
- **T2** — чат: плавающее окно на рабочем столе у клиента + снизу у сотрудника (P1).
- **T4** — дизайн в стиле onlinepbx.ru (P1).
- **T5** — иконки/ярлыки onlinePBX (P2).
- **T7** — скриншоты сотрудника → в Документы/RemoteDeskPBX (P1).
- **T8** — авто-скриншоты каждые 30с → на сервер (P0, фичи ещё нет; связать с sessionId).
- **P** — объединить репозитории app+server; перевести Render на RemoteDeskPBX.

### Как проверить текущий прод
- `GET https://remotedeskpbx-server.onrender.com/` → есть `"server":"express-v2"`.
- Админка `/admin` (admin123): вкладки переключаются с индикатором; после реальной сессии сотрудник виден в списке.
- Новые `.exe`: `release/RemoteDeskPBX-Client-2.0.0.exe`, `release/RemoteDeskPBX-Employee-2.0.0.exe`.

### Хэндофф
Следующая рекомендуемая задача: **T7** или **T8** (скриншоты) либо **T1** (сохранение пароля). Брать по одной, через новый чат с `SESSION_PROMPT.md`.
